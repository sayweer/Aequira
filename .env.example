# Public runtime selection. Secrets must never be stored in this file.
AEQUIRA_NETWORK=preprod
AEQUIRA_PROOF_SERVER_URL=http://127.0.0.1:6300

# Reserved for the encrypted private-state provider added in the next CLI slice.
AEQUIRA_PRIVATE_STATE_DIR=.private-state/aequira
