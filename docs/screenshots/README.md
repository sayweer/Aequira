# Screenshots

The README embeds these five files. Capture each one and drop it in here with the
exact filename.

| File                    | What to capture                                      |
| ----------------------- | ---------------------------------------------------- |
| `compile-output.png`    | `pnpm compact:build` with the circuits listed        |
| `deployed-contract.png` | the app after deployment, contract address visible   |
| `tests-passing.png`     | `pnpm test` showing the passing totals               |
| `ci-passing.png`        | a green GitHub Actions run for both jobs             |
| `privacy-panel.png`     | the disclosure panel: commitment public, score local |
